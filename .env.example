GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override which Gemini model is used for each task.
# Useful when one model's free-tier quota is exhausted.
# Defaults: gemini-2.0-flash for all tasks.
# PARSE_MODEL=gemini-2.0-flash
# TOUR_MODEL=gemini-2.5-flash
